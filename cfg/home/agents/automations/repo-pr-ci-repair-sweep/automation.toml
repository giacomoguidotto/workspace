# Declarative source. The live materialization is
# ~/.codex/automations/renovate-pr-ci-fixer/automation.toml.
schema_version = 1
# Legacy runtime ID preserved so materialization updates the existing task in place.
id = "renovate-pr-ci-fixer"
kind = "cron"
name = "Repo PR CI Repair Sweep"
status = "ACTIVE"
rrule = "FREQ=DAILY;BYHOUR=8;BYMINUTE=45;BYSECOND=0"
timezone = "Europe/Rome"

execution_profile = "balanced/high"
model = "gpt-5.6-terra"
reasoning_effort = "high"
execution_environment = "local"
destination = "local"

target_project = "dev"
cwd = "~/dev"
prompt_file = "prompt.md"
