# Personal Rules

- Never use the `question` tool / interactive question prompter. If you need clarification or a decision, ask in plain text in your response instead. When there are options, list them in plain text and give your recommended choice (or choices, if more than one).
